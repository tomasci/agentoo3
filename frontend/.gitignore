node_modules/
dist/
*.tsbuildinfo

# Generated during installation by `bun run codegen`, from the openapi.json the
# backend on that machine produces. Deliberately not committed: a snapshot taken
# on a developer's checkout can silently disagree with the deployed backend.
src/shared/api/generated/
