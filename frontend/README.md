# frontend

Web UI, run with Bun. The installer provisions Bun and Node; it does not yet
install dependencies or build — that will be a later installer step
(`60-setup-app.sh`).

Manual setup for now:

    bun install
    bun run dev
